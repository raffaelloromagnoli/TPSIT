#include <stdio.h>

void enqueue(struct queue_node **head,
        struct queue_node **tail, struct  queue_node **element){
    if (is_empty)
}

int main() {
    printf("Hello, World!\n");
    return 0;
}
